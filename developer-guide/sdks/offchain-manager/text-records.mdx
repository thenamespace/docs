---
title: "Text records"
description: "Read and write ENS text records on a subname."
sidebarTitle: "Text records"
icon: "address-card"
---

<Note>
  Text records store public profile details such as a display name, website, avatar, or social handle. Do not store secrets in text records.
</Note>

These methods allow you to manage text records for a given ENS subname. You can fetch, add, update, or delete individual records.

## Add or update a text record

Add or update a text record.

```typescript
await client.addTextRecord(
  'alice.example.eth',
  'description',
  'Namespace community member',
);
```

**Parameters:**
- `subname` - Full ENS subname
- `key` - Text record key (e.g., "description")
- `value` - Value to associate with the key

## Delete a text record

Delete a text record.

```typescript
await client.deleteTextRecord('alice.example.eth', 'description');
```

## Get all text records

Get all text records for a subname.

```typescript
const records = await client.getTextRecords('alice.example.eth');
// Returns: Record<string, string>
```

## Get one text record

Get a specific text record by key.

```typescript
const response = await client.getTextRecord('alice.example.eth', 'description');
const description = response.record;
```

**Return Type:**
```typescript
interface GetRecordResponse {
  record: string;
}
```
