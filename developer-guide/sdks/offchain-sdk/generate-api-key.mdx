# Generate API key

To interact with the offchain system, an API key is required for each ENS name. Here's how you can generate and manage API keys:

### Generating an API Key

<Hint type="info">
  The best and easiest way to generate an API key is through our Dev Portal (
  <a href="https://dev.namespace.ninja/" target="_blank" rel="noopener">
    https://dev.namespace.ninja/
  </a>
  ). See the Loom demo video below on how to do it. 👇
</Hint>

<Embed
  url="https://www.loom.com/share/b19d490f44d741ed93c70cdbd998a0fd?sid=ae379437-6897-460c-bbd2-0f253da5e7dd"
  alt="Loom video showing how to generate an API key in the Namespace Dev Portal"
/>

### Using the API Key

Once the API key is generated, you can set it for an ENS name using the setApiKey method:

```typescript
client.setApiKey("example.eth", "your-api-key");
```

This ensures all subsequent requests for the ENS name use the provided API key.
